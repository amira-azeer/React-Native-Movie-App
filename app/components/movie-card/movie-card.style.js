import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  Text: {
    color: "white",
    fontSize: 20,
    fontWeight: "600",
    marginBottom: 5,
    margin: 10,
  },
});

export default styles;
