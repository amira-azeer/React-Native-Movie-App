import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    MainView: (height, width) => ({ 
        height: height, 
        width: width, 
        position: 'absolute',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems:'center'
    }),
    ProgressCircle: {
        
    }
});

export default styles;